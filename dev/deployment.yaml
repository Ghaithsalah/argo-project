apiVersion: apps/v1
kind: Deployment
metadata:
  name: myfirst-argo-app
spec:
  selector:
    matchLabels:
      app: myfirst-argo-app
  replicas: 2
  template:
    metadata:
      labels:
        app: myfirst-argo-app
    spec:
      containers:
      - name: myfirst-argo-app
        image: nanajanashia/argocd-app:1.2
        ports:
        - containerPort: 8080
